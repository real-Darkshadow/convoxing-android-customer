<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:background="@drawable/bottom_sheet_round_white"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingTop="16dp"
    android:paddingBottom="36dp">

    <!-- Title Text -->
    <TextView
        android:id="@+id/titleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/gilroy_semibold"
        android:paddingBottom="8dp"
        android:text="You are almost there!"
        android:textColor="@color/secondary"
        android:textSize="16sp" />

    <!-- Description Text -->
    <TextView
        android:id="@+id/descriptionTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="@font/gilroy_medium"
        android:paddingBottom="24dp"
        android:text="If you skip our talk now, you'll only see me tomorrow"
        android:textColor="@color/text_gray"
        android:textSize="14sp" />

    <!-- Primary Action Button -->
    <TextView
        android:id="@+id/btnContinue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:background="@drawable/primary_button_bg_14"
        android:backgroundTint="@color/primary_yellow"
        android:fontFamily="@font/gilroy_semibold"
        android:gravity="center"
        android:padding="12dp"
        android:text="Let's continue the conversation"
        android:textColor="@color/secondary" />

    <!-- Secondary Action Button (Cancel) -->
    <TextView
        android:id="@+id/btnExit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:clickable="true"
        android:focusable="true"
        android:fontFamily="@font/gilroy_medium"
        android:gravity="center"
        android:text="Yes, I'll see you tomorrow!"
        android:textColor="#AC606C"
        android:textSize="13sp" />

</LinearLayout>